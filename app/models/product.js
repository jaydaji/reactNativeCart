// simply for an example for now, as the products supplied are already in a data array
class Product {
  constructor(name, price) {
    this.name = name;
    this.price = price;
  }
}

export default Product;
